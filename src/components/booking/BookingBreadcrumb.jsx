import Breadcrumb from '@/components/common/Breadcrumb'

/** @deprecated import Breadcrumb from '@/components/common/Breadcrumb' directly instead. */
export default function BookingBreadcrumb(props) {
  return <Breadcrumb {...props} />
}

